name: Auto-merge
on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  call:
    uses: EdgeVector/.github/.github/workflows/auto-merge.yml@main
    secrets: inherit
