name: discord-notify.yml (mirror-inert)

# GitHub is a read-only mirror. LastGit is SoT; AWS deploy is on the forge host.
# This workflow does nothing automatically.

on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub mirror — no Actions. Use LastGit + forge-host deploy-pipeline."
